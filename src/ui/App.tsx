/**
 * Root UI component. Placeholder until the first screens are designed
 * (docs/project-overview.md — design mock comes before real screens).
 */
export function App() {
  return <main>vim-dojo</main>;
}
